let text = "Idzie kominiarz po drabinie fiku miku już w kominie!";

const textEdit = (a,b) => a


const processText = (text, { uppercase = false, reverse = false, removeSpecialChars = false } = {}) =>{
    if(uppercase == true){
        text.uppercase();
    }
    
} 

processText('Hello world', {uppercase}, {reverse})